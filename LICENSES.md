# Licensing

`x201t-wacomd` is licensed as BSD-2-Clause. See [LICENSE](LICENSE).

Source files carry SPDX identifiers where the file format allows it.

The included FreeBSD port metadata declares `BSD2CLAUSE` to match the daemon
source.
